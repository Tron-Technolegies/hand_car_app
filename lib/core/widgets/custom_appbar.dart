import 'package:flutter/material.dart';

class CustomAppbar extends StatelessWidget {
  const CustomAppbar({super.key});

  @override
  /// Build a custom app bar.
  ///
  /// This widget returns a placeholder for now, it will be implemented later.
  ///
  /// The [context] parameter is used to access the [Theme] of the app.
  Widget build(BuildContext context) {
    return AppBar(
      
    );
  }
}